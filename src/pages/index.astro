---
import Layout from '../layouts/Layout.astro';
import GrilledMeatVeg from '../components/googleSheets/GrilledMeatVeg.astro';
import Skewers from '../components/googleSheets/Skewers.astro';
import Stews from '../components/googleSheets/Stews.astro';
import Rice from '../components/googleSheets/Rice.astro';
import Cakes from '../components/googleSheets/Cakes.astro';
import Casseroles from '../components/googleSheets/Casseroles.astro';
import Drinks from '../components/googleSheets/Drinks.astro';
import Pasta from '../components/googleSheets/Pasta.astro';
import Soups from '../components/googleSheets/Soups.astro';
---

<Layout title='Welcome to the ScumYum'>
  <main class='specialElite'>
    <div class='container'>
      <h1>
        <div class='logo'>
          <div class='scum'>Scum</div>
          <div class='yum'>Yum</div>
        </div>
        Restaurant Menu
      </h1>
<!--- <div class="copy">Here we dont need your money, but we need your balls! All prices in puppet eye balls!</div> --->
      <div id='menu' class='menu'>
        <GrilledMeatVeg />
        <Skewers />
        <Drinks />
        <Casseroles />
        <Pasta />
        <Rice />
        <Soups />
        <Stews />
        <Cakes />
      </div>
    </div>
  </main>
</Layout>

<style is:global>
  :root {
  }
  body {
    background: url('../assets/images/black-chalkboard.webp') no-repeat center
      center fixed;
    -webkit-background-size: cover;
    -moz-background-size: cover;
    -o-background-size: cover;
    background-size: cover;
    min-height: 100vh;
  }
  main {
    display: flex;
    justify-content: center;
    align-items: center;
    color: var(--body-ftclr);
    font-size: clamp(18px, 1.5vw, 22px);
    line-height: 1.5;
    padding: 1em;
  }
.copy{
    text-align: center;
    font-size: 1.25em;
    margin-bottom: 1em;
}
  h1 {
    text-align: center;
    margin: 2rem 0;
    font-size: 2.5rem;
  }
  .logo {
    text-transform: uppercase;
    font-size: 1.75em;
    line-height: 0.4;
    letter-spacing: -0.05em;
  }
  .logo:before,
  .logo:after {
    content: '';
    display: inline-block;
    background-color: var(--brand-clr);
    border-top-left-radius: 0.125em;
    border-top-right-radius: 0.125em;
    border-bottom-left-radius: 0.125em;
    border-bottom-right-radius: 0.125em;
  }
  .logo:before {
    width: 2.25em;
    height: 0.75em;
    border-top-left-radius: 1.5em;
    border-top-right-radius: 1.5em;
    border-bottom-left-radius: 0.25em;
    border-bottom-right-radius: 0.25em;
  }
  .logo:after {
    width: 2.25em;
    height: 0.35em;
    border-top-left-radius: 0.125em;
    border-top-right-radius: 0.125em;
    border-bottom-left-radius: 0.125em;
    border-bottom-right-radius: 0.125em;
  }
  .logo > .scum {
    line-height: 1.2;
  }
  .logo > .yum {
    font-size: 1.25em;
  }
  .menu {
    columns: 15rem 3;
    column-gap: 4rem;
    column-rule: 1px solid var(--body-ftclr);
  }
  .menu > div {
    display: flex;
    justify-content: center;
    margin-top: 2rem;
    position: relative;
  }
  .menu > div:first-of-type {
    margin-top: 0;
  }
  .category {
    max-width: 32rem;
    text-align: left;
    width: 100%;
  }
  .title {
    text-align: center;
    margin-top: 0;
    margin-bottom: 0;
    font-size: 1.75em;
  }
  .item {
    display: inline-block;
    width: 100%;
    margin-bottom: 1rem;
  }
  .item > .title {
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-size: 1.25em;
    margin: 0;
  }
  .title h3 {
    margin: 0;
    text-align: left;
  }
  .title .price {
    font-size: 1em;
    font-weight: 600;
  }
  P {
    margin: 0;
    font-size: 0.75em;
  }
</style>
